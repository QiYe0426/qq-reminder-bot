# Metadata v2 Phase 2.5-B1.1.6 Audit Report

## 1. 当前状态

qq-reminder-bot 已完成 Metadata v2 compatibility resolver，以及 Audit、Confirmation
decision、Idempotency enable、feature-flagged Authorization、Handler execution timeout
等分阶段迁移。Output Governance 当前具备 UTF-8 canonical JSON bytes measurement、默认
关闭的 enforcement framework、隔离 `TextReducer` 原型、capability model 和静态
Capability Inventory。

- `AGENT_OUTPUT_BUDGET_ENFORCEMENT` 默认关闭。
- `OUTPUT_BUDGET_REDUCERS` 为空。
- `OUTPUT_REDUCER_CAPABILITIES` 为空。
- Inventory 覆盖当前 14 个注册 AgentTool，但没有 enabled capability。
- 工作区修改未提交，未运行全量测试。

## 2. 架构变化

Output Budget framework 在 Handler 结果 normalize 后测量 canonical ToolResult 的 UTF-8
JSON bytes。只有 flag 开启、预算超限且存在显式 reducer 时才会尝试 reduction；失败或
非法结果回退原结果。`TextReducer` 只处理配置的 `data` 路径，按 Unicode code point
裁剪并 canonical rebuild。

本阶段新增的 `OutputReducerInventoryItem` 是 frozen audit record：

```text
Registered AgentTool
  -> explicit canonical data structure audit
  -> candidate paths / blocked paths
  -> risk level / review status
  -> future disabled capability proposal
```

Inventory 不连接 Gateway，不创建 reducer，不从 ToolResult 自动推断路径。

## 3. Runtime 不变量

- Authorization 顺序和 legacy AND metadata 行为不变。
- Confirmation decision 和 token lifecycle 不变。
- Idempotency claim、cache、unknown 和 replay 不变。
- Handler 不因 Inventory 重复执行。
- first result、cache 和 replay 的既有一致性不受影响。
- 用户可见输出不变。

## 4. 安全约束

- candidate、blocked 和 output structure 路径必须位于 `data` namespace。
- candidate 与 blocked 路径不得重叠。
- error、execution、idempotency 和 confirmation 顶层语义不能成为候选。
- resource/file/task/message ID、URL、delivery state 和配置状态被明确阻止或拒绝。
- Inventory 与 capability 均为 frozen dataclass，registry 为只读映射。
- Inventory deterministic，不扫描字符串，无数据库、网络或其他副作用。

## 5. 文件修改列表

本阶段代码与测试：

- `plugins/agent_tools/output_reducer_inventory.py`
- `tests/test_agent_tool_output_reducer_inventory.py`

同步文档：

- `README.md`
- `AGENTS.md`
- `docs/agent_handoff.md`
- `D:\猎bot\审计报告07111907.md`

工作区还包含前序 Phase 2 未提交修改；本报告没有把它们描述为本阶段新增。

## 6. 测试结果

Inventory、Capability、TextReducer、Output Budget framework/shadow 和 Idempotency policy
source 定向回归：63 passed in 5.97s。未运行全量测试。

## 7. 当前风险

- Inventory 基于静态代码审计，尚未使用生产样本验证所有可选输出分支。
- `get_group_profile` 和 `get_member_profile` 的嵌套 summary 仍为 REVIEW。
- 日报和语义图 summary 虽为 ALLOW 候选，但尚未执行 Shadow Reduction。
- 固定结构本身超过预算时仍只能安全回退。
- 当前没有 production reducer；若未来配置错误，仍需 registry/capability 一致性门禁。

## 8. 下一阶段计划

Phase 2.5-B1.2 应保持非 enforcement：对 ALLOW 候选构造测试样本，执行 Shadow
Reduction，记录 before/after bytes、结构差异、protected-field 比较和 replay 一致性。
REVIEW 项必须先完成 schema 人工确认。只有 shadow inventory 无安全差异后，才可单独
评审 disabled capability 到 production reducer 注册的迁移。

## Git Checkpoint Record

- Checkpoint commit: `56db056c9b8d795764dd818214cca4108eeb3c6f`
- Commit message: `feat(agent-runtime): complete metadata v2 output governance foundation`
- Branch: `feature/agent-runtime-v3`
- Push status: successfully pushed to `origin/feature/agent-runtime-v3`
- Validation: `git diff --check` passed; 187 targeted tests passed in 9.03s;
  full test suite was not run.
- Working tree after checkpoint push: clean; local/remote divergence was `0 0`.
- Next phase: B1.2 Shadow Reduction has not started.
